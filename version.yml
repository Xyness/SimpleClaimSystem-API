# Version
version: 2.6.8

# Update date
date: 17/07/2026

# Update notes
update-notes:
- "Fixed several config.yml options being silently ignored (their values are now applied): claims.respawn-in-claim.enabled, claims.claim-back.only-own-claims, claims.confirmation.owner, claims.default.warp / visitPrice / hologram, database.hikari.leak-detection-ms and audit-logs.flush-interval-seconds / batch-size"
- "Fixed the Home button in claim menus requiring the obsolete scs.claim.home permission; it now uses scs.claim.menu so it works for every player"
- "Added the missing scs.bypass.interact_glow_berries staff-override bypass node"
