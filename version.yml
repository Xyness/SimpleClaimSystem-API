# Version
version: 2.1.4

# Update date
date: 01/04/2026

# Update notes
update-notes:
- Custom roles support with String-based role system
- Add customRoles list, getAllRoles(), getNextRole() to Claim
- Add ClaimRole.isDefault() utility method
- Add ROLE_CHANGE action to ClaimMemberEvent
- Update all API signatures to use String roles
- Keep backward-compatible methods accepting ClaimRole enum
