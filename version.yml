# Version
version: 2.1.3

# Update date
date: 01/04/2026

# Update notes
update-notes:
- Change Claim members/permissions maps from ClaimRole enum to String-based keys
- Add customRoles list, getAllRoles(), getNextRole() to Claim
- Add ClaimRole.isDefault() utility method
- Update ClaimMemberEvent/ClaimPermissionChangeEvent to use String role with backward compat
- Update SCS_API signatures to use String roles
- Keep backward-compatible methods accepting ClaimRole enum
