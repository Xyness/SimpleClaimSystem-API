# Version
version: 2.6.6

# Update date
date: 02/07/2026

# Update notes
update-notes:
- "Fixed permission claim/chunk limits being ignored (scs.limit.chunks.* now grants unlimited)"
- "Fixed spawner/block chunk-limit false positives caused by in-memory counter drift"
- "Added pay-to-claim beyond the chunk limit (claims.economy.pay-over-limit)"
- "Added configurable /claim see particle duration (claims.see-duration-seconds)"
- "Added per-event Discord webhook toggles (discord-webhook.events)"
